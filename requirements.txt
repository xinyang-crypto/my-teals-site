# Python dependencies for Telar
# Used by GitHub Actions for data processing and IIIF generation

# Data processing
pandas>=3.0.3
pyyaml>=6.0
markdown>=3.10.2
jinja2>=3.1.0

# Story encryption (protected stories feature)
cryptography>=48.0.0

# IIIF tile generation
iiif>=1.0.10

# Image processing
Pillow>=12.2.0

# PDF rendering (multi-page PDF objects)
PyMuPDF>=1.27.2.3

# HEIC/HEIF support (iPhone photos)
pillow-heif>=1.3.0

# Testing (development only)
pytest>=9.0.3
pytest-cov>=7.1.0
playwright>=1.40.0
pytest-playwright>=0.8.0
